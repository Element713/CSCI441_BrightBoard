{
  "name": "brightboard-monorepo",
  "private": true,
  "workspaces": [
    "brightboard-frontend",
    "server"
  ],
  "scripts": {
    "start": "node server/server.js",
    "heroku-postbuild": "cd brightboard-frontend && npm install && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.0.0",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.16.1"
  }
}
